export const TOOLBAR_HEIGHT = { xs: 56, sm: 64 } as const;

export const MAIN_PADDING_X = { xs: 1.5, sm: 2, md: 3 } as const;

/** Компенсация горизонтального padding у main для full-bleed шапки. */
export const MAIN_PADDING_X_NEG = { xs: -1.5, sm: -2, md: -3 } as const;
